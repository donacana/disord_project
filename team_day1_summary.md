# Team C (연예·문화) — Day 1 작업 정리

작성일: 2026-09-08

---

## 11. 매일 체크리스트 (원본 6항목 통합)

| 항목 | 상태 | 담당 |
|---|---|---|
| 오늘자 데이터 수집 성공 (건수 기록) | ✅ 1,140건 | Data / Backend |
| /stats 에 마지막 수집 시각 반영 | ⬜ 확인 필요 | Bot이 반영 여부 재확인 중 |
| 샘플 질문 3개 Bot으로 테스트 | ✅ 1건 실제 확인, 추가 진행 중 | Bot — !ask 질문·답변·출처 정상 작동 확인 (아래 사례 참고) |
| 답변에 출처 URL 포함 여부 확인 | ✅ 확인됨 | 출처 링크·수집 시각·분야 태그까지 정상 표기 확인 |
| Discord #team 채널에 작업 로그 1회 이상 | ✅ | 전체 |
| 막힌 이슈는 #help에 공유 (혼자 붙잡지 않기) | ✅ | Data(.env 노출) / Bot(공공데이터 API 403) |

---

## 실제 작동 확인 사례 (Day 1)

**질문(이혜림)**: `!ask 안치환이 국가폭력과 평화에 관해 어떤 메시지를 전했어?`

**team C bot 답변**:
> 안치환은 국가폭력과 평화에 대해 "전쟁도 마찬가지겠지만 국가의 폭력은 가장 야만적인 삶의 방해물이라고 생각한다"며, "이런 게 없는 세상이 삶의 평화를 가져다준다고 본다"고 강조했습니다. 그는 국가폭력이 없어진 세상이 평화를 가져올 것이라는 메시지를 전하며, 저항 가요의 필요성을 언급했습니다 [1].
>
> 또한, 그는 "내란이 종식되는 날 신나게 놀아보려 했는데, 그 시기가 빨리 온 것 같다"며, 일상에서 사람들을 위로하는 노래가 필요하다고도 말했습니다. 이는 그가 음악을 통해 사람들에게 긍정적인 메시지를 전달하고자 하는 의지를 나타냅니다 [1].
>
> **출처**
> 1. 안치환 "국가폭력 없어야 삶에 평화...노래로 '토닥토닥'할 때"
>    수집 시각: 2026-09-08T09:01:53.085227
>
> 분야: `ent_culture`

**의미**: Data(수집) → Backend(임베딩·검색·답변 생성) → Bot(Discord 표시)까지 전체 파이프라인이 실제로 한 번에 작동함을 확인. 출처 인용 번호, 링크, 수집 시각, 분야 태그까지 설계했던 답변 형식이 그대로 반영됨.

**남은 검증**: 답변 내용이 원문 기사를 얼마나 정확히 반영했는지(왜곡 없는 인용인지)는 Day 3~4에 원문 대조로 추가 확인 필요.

---

## Data 담당 — 오늘 진행 내역

**완성**
- DB 스키마 확인 및 정리 (trend_briefs 포함 11개 테이블)
- `collector.py` 완성 — RSS 다중 소스(연합뉴스·한국경제·스포츠경향 4섹션) 수집
  - 무관 기사 필터링(부고·공정위·포토 캡션 등)
  - 카테고리 자동 분류 규칙 개선
  - 죽은 링크(한국경제 User-Agent 이슈) 해결
- 팀 확장 구조 세팅 — `sources_rss.py`(RSS 확장용), `sources_naver.py`(네이버 API, Backend와 협업)
- GitHub Actions `collect.yml` 등록 — 매일 08:00 KST 자동 수집, RSS/네이버 2단계 분리
- RSS 수집 200건+ 확보 (전체 1,140건 중 RSS 기여분)

**보안 조치**
- `.env` 파일 GitHub 노출 발견 → git 추적 제거(`git rm --cached`), Neon DB 비밀번호 재발급
- `.gitignore` 강화 (`.env`, `.idea/`, `__pycache__` 등)

**협업**
- GitHub merge 충돌 2건 해결 (`.gitignore`, `.idea/modules.xml`)
- 팀원 A(RSS 소스 확장), 팀원 B(네이버 API) 작업 뼈대 전달 및 연동

---

## Backend 담당 — 오늘 진행 내역

**데이터 수집**
- NAVER API HUB 뉴스 검색 연동 + 본문 추출 파이프라인 구현
- Neon PostgreSQL `articles` 저장 정상 확인 — **현재 기사 1,140건**
- 중복 기사 방지 3중 구현
  - URL 완전 일치 중복 제거
  - URL이 달라도 본문 전체가 같으면 중복 제거 (content_hash)
  - 기존 DB 기사와도 중복 검사 후 제외
- `sources` 테이블 네이버뉴스 `source_id=11` 연동, `articles.source_id NOT NULL` 오류 해결
- DB 연결 정상 확인 (`SELECT 1` → `(1,)`), `DATABASE_URL` 문제 해결

**임베딩**
- `text-embedding-3-small` 1536차원 정상 검증, NULL 임베딩 0건
- `article_embeddings.article_id` PK 정상, RAG용 JOIN 정상 동작 확인
- GitHub Actions `embed-articles.yml` 구현 — 매시간 정각 자동 임베딩
  - GitHub Secrets에 `DATABASE_URL`, `OPENAI_API_KEY` 등록, 실제 Actions 실행 성공
  - 자동 반영 확인: 초기 6건 → 16건 → **335건**
- 대량 임베딩 개선: 50건 배치 실패 확인 → 10건 배치로 조정, 실패 기사 있어도 전체 중단 안 되게 개별 재시도/skip 구조 설계

**협업**
- `backend/db.py` merge 충돌(`<<<<<<< HEAD` 마커) 해결
- GitHub Actions 문법 오류 해결, main pull/merge/push 정상 진행
- Discord `/stats` 확인 완료 — 기사 1,140건 / 임베딩 335건 / 수집처 7개 / 마지막 수집 시각 반영

---

## Bot 담당 — 오늘 진행 내역

**임베딩 관련**
- `/health`에서 DB 연결 정상 확인
- 수동 임베딩 기능 구현
  - `backend/openai_client.py`: `embed_articles()` 함수 추가
  - `backend/embed_articles.py`: 미임베딩 기사 일괄 처리 구현
- 실제 기사 3건 임베딩 테스트 완료 — 벡터 1536차원 확인, DB `is_embedded = true` 확인

**Discord Bot**
- Bot 연결 및 질문·답변 동작 테스트 완료
- 작업 내용 GitHub main 브랜치 반영 완료

**수집 소스 확장**
- 추가 RSS 소스 조사 및 수집 테스트
  - 동아일보 RSS 정상 수집 확인
  - 조이뉴스 RSS는 수집 안 돼서 제외
- 공연 일정 공공데이터 수집 작업 진행 중
  - `sources_public_event.py` 작성
  - 공공 API XML 응답 구조 확인
  - 인증키 이중 인코딩으로 인한 403 오류 수정 중 (막힌 이슈)

**확인 필요 항목 (본인 정리)**
- `/stats` 마지막 수집 시각 반영 여부 확인 필요
- Bot 샘플 질문 3건 및 출처 URL 포함 여부 추가 확인 예정

---

## 팀 전체 현재 상태 (Day 1 종료 시점)

| 지표 | 수치 |
|---|---|
| 전체 기사 수 | 1,140건 |
| 임베딩 완료 | 335건 (진행 중, 매시간 자동 처리) |
| 수집처 | 7개 (RSS 6개 + 네이버뉴스), 동아일보 추가 확인됨 |
| DB 테이블 | 11개 (trend_briefs 포함) |
| GitHub Actions | collect.yml(수집), embed-articles.yml(임베딩) 2개 정상 작동 |
| Discord Bot | 연결 및 기본 질문·답변 동작 확인 |

---

## 오늘 발견된 막힌 이슈 2건

1. **Data** — `.env` 파일이 GitHub에 노출됨 → git 추적 제거 + DB 비밀번호 재발급으로 해결
2. **Bot** — 공공데이터 API 인증키 이중 인코딩으로 403 오류 → 수정 진행 중 (Day 2 이어서)

---

## Day 2 진행 전 확인 사항

1. 공공데이터 API 403 오류 해결 (Bot)
2. `/stats` 마지막 수집 시각 실제 반영 여부 최종 확인 (Bot)
3. 샘플 질문 2건 추가 테스트 (원문 대조로 답변 정확도까지 확인)
4. Backend 네이버 API 연동과 Data 쪽 `sources_naver.py` 작업이 중복인지 확인 필요
5. Backend/Bot 양쪽에서 임베딩 관련 파일(`embed_articles.py`)을 각자 작업한 것으로 보임 — 최종 반영본 확인 필요
6. 임베딩 335/1,140건 — 매시간 자동 처리 중이므로 Day 2 아침 재확인
