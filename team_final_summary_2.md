# Team C (연예·문화) — 발표 전날 최종 작업 정리

작성일: 2026-09-09

---

## 11. 매일 체크리스트

| 항목 | 상태 | 비고 |
|---|---|---|
| 오늘자 데이터 수집 성공 (건수 기록) | ✅ 7,038건 | RSS + 네이버 + 문화포털 + TMDB |
| /stats 에 마지막 수집 시각 반영 | ✅ | Discord `/stats`에서 확인됨 |
| 샘플 질문 3개 Bot으로 테스트 | ✅ | `#봇-테스트` 채널에서 실시간 확인 |
| 답변에 출처 URL 포함 여부 확인 | ✅ | citation 검증 로직 추가 반영 |
| Discord #team 채널에 작업 로그 1회 이상 | ✅ | 전체 |
| 막힌 이슈는 #help에 공유 (혼자 붙잡지 않기) | ✅ | 문화포털 인증 오류, 임베딩 배치 등 공유 완료 |

---

## Data 담당 (본인) — 오늘 진행 내역

**수집 파이프라인 완성**
- `collector.py` — RSS 6개 소스(연합뉴스·한국경제·스포츠경향 4섹션) 수집, 무관 기사 필터, 카테고리 자동 분류
- `keyword_extractor.py` — 제목에서 인물·작품명 추출 로직 구현, 기존 2,518건 백필 완료
- `collector.py`에 keywords 자동 생성 연결 (신규 수집분부터 자동 반영)

**데이터 품질 문제 발견 및 해결**
- 공공행사 콘텐츠가 200자 미만이라는 이유로 586건이 부당하게 스킵되던 버그 발견·수정
- `categories` 테이블이 비어있던 것을 발견 → 8개 카테고리 등록 → FK 제약 추가로 오타 방지 장치 마련
- 인물별 기사 편차 문제 확인(장원영 2건, 임영웅 11건 등) → keywords 빈도 기반 인물 검색어 22개 선별·추가

**보안 사고 처리**
- `.env` 파일 GitHub 노출 발견 → git 추적 제거, DB 비밀번호 재발급
- `.gitignore` 강화 (`.env`, `.idea/`, `__pycache__` 등), 관련 merge 충돌 2건 해결

**팀 협업 구조**
- `sources_rss.py`, `sources_naver.py` 팀 확장용 뼈대 제공 → 팀원 A(RSS), 팀원 B(네이버) 각자 작업 후 병합
- GitHub Actions `collect.yml` 등록 — RSS/네이버 다단계 실행, 매일 08:00 KST 자동 수집 확인
- `.env` 위치 문제(collector 폴더 내 `.env` 부재) 등 팀원 실행 오류 디버깅 지원

**최종 데이터 현황**
- 전체 기사 7,038건 / 임베딩 6,077건
- 수집처 8개 (RSS 6 + 네이버뉴스 + 문화포털, TMDB 반영 진행 중)

---

## 공공API/RSS 확장 담당 — 오늘 진행 내역

**문화포털 공공API 연동**
- `SERVICE_KEY_IS_NOT_REGISTERED_ERROR` 원인 확인 — 포털 승인과 별개로 "운영계정 신청"이 추가로 필요했음을 발견
- 운영계정 신청 및 승인 후 API 정상 호출 확인
- 이중 인코딩 문제(`serviceKey`가 params에 있어 자동 재인코딩됨) 해결 — URL에 직접 조립하는 방식으로 수정
- 문화포털 데이터를 기존 수집 구조와 동일한 `list[dict]` 형태로 구성 (`sources_public_event.py`)

**RSS 소스 확장**
- SBS·조선일보·뉴시스·국민일보 연예 RSS 추가
- RSS별 수집 건수 및 파싱 오류 여부 확인

**TMDB API 연동**
- TMDB 토큰 발급 및 환경변수 설정
- `sources_tmdb.py` 구현 — 영화·드라마·예능 데이터 수집 확인
- TMDB 반환값을 기존 수집 구조와 동일한 `list[dict]` 형태로 구성
- RSS 및 TMDB 변경 사항을 hyerim 브랜치 및 main 브랜치에 반영

---

## Backend 담당 — 오늘 진행 내역

**검색 품질 개선**
- 벡터 검색에 키워드·제목·최신성·entity·intent·category를 함께 반영하는 방식으로 개선
- 후보 문서 수 확대, similarity 기준 완화, 제목-entity 일치 시 우대
- 중복 제거, 출처 다양성, 최근 기사 우선 적용 로직 추가

**답변 신뢰성 강화**
- 검색 자료에 없는 정보 생성 방지 로직 추가
- citation·숫자·명칭 검증 로직 추가 — 근거 없는 문장만 제거하고 근거 있는 문장은 유지
- 안전한 paraphrase와 부분 답변 허용
- 실제 사용한 출처만 반환하도록 수정 (이전에 발견된 "인용 안 된 출처가 그대로 노출되는 문제" 해결로 보임)

**질문 이해 기능 강화**
- `QueryAnalysis` 구조 추가
- LLM을 질문 해석에 사용해 entity·intent·time_range·keywords 추출
- 구어체·자연스러운 표현 대응
- LLM 실패 시 기존 rule 기반 분석으로 fallback

**네이버 뉴스 수집 확대**
- 검색어 10개 → 44개로 확대 (Data 팀 인물 검색어 22개 포함)
- 검색어당 1페이지 → 최대 10페이지, `start=1~901` pagination 적용 (검색어당 최대 1,000건 후보 조회)
- 2026년 기사 필터, 중복 검사, 오류·호출 통계 강화
- 신규 기사는 기존 embedding 파이프라인에서 처리

**Trend Ranking 구현**
- "요즘 누가 유명해?" 같은 질문을 일반 벡터 검색과 분리 처리
- 최근 7일 기사 최대 1,000건 집계
- 언급량 + 언론사 다양성 + 최근성으로 순위 계산
- 대표 기사와 citation을 함께 반환
- `entities` 테이블이 비어있어 현재는 기사 제목 기반 entity 추출로 대체 사용 중

**테스트 및 현재 상태**
- `py_compile` 통과
- 기존 + 신규 테스트 총 43개 통과
- 실제 DB 연결 및 Trend 집계 확인
- DB 상태: articles 7,038건 / embeddings 6,077건

---

## 오늘 발견된 주요 이슈와 해결 과정

| 이슈 | 발견 경위 | 해결 |
|---|---|---|
| `.env` GitHub 노출 | git status 확인 중 발견 | 추적 제거 + DB 비밀번호 재발급 |
| 공공행사 200자 필터 오작동 | 586건 스킵 로그 확인 | content 사전 채움 시 길이검사 예외처리 |
| `categories` 테이블 비어있음 | FK 제약 시도 중 발견 | 8개 카테고리 재등록 + FK 추가 |
| 문화포털 인증키 이중 인코딩 | 403 에러 반복 발생 | serviceKey를 params 밖으로 분리, URL 직접 조립 |
| 문화포털 운영계정 별도 승인 필요 | 인코딩 수정 후도 403 지속 | 포털에서 운영계정 추가 신청·승인 |
| 인물별 기사 편차 (장원영 2건 등) | 봇 답변 얕음 지적 | keywords 빈도 기반 인물 검색어 추가 |
| 인용 안 된 출처가 답변에 노출 | 실제 질의응답 테스트 중 발견 | 실제 사용 출처만 반환하도록 Backend 수정 |
| 무관한 인물이 검색 결과에 섞임 | 아이브/장원영 질문 테스트 중 발견 | 하이브리드 검색(entity/keyword) 반영으로 개선 |

---

## 최종 데이터 현황

| 지표 | 수치 |
|---|---|
| 전체 기사 | 7,038건 |
| 임베딩 완료 | 6,077건 (약 86%) |
| 수집처 | RSS 10개(연합뉴스·한국경제·스포츠경향4·SBS·조선일보·뉴시스·국민일보) + 네이버뉴스 + 문화포털 + TMDB |
| DB 테이블 | 11개 (trend_briefs 포함), categories FK 적용 |
| GitHub Actions | collect.yml(RSS/네이버/문화포털 단계별), embed-articles.yml(매시간) 정상 작동 |
| 백엔드 테스트 | 43개 통과 |

---

## 내일(발표일) 확인 사항

1. TMDB 데이터 collector.py 파이프라인 최종 연결 확인 (GitHub Actions 반영 여부)
2. `collect.yml`에 `PUBLIC_DATA_API_KEY`, `TMDB_ACCESS_TOKEN` Secrets 반영 확인
3. 아침 자동 수집·임베딩 정상 작동 여부 확인 (`/stats`)
4. 샘플 질문 최종 리허설 (Trend Ranking 포함)
5. 발표 자료용 캡처: `/stats`, 실제 질의응답 예시, 오늘 겪은 이슈 회고 슬라이드
